app.factory("<%= directiveName %>", ['$http', '$q','RESOURCE', function ($http, $q,RESOURCE) {
	return {
		restrict : '',
		templateUrl : '',
		link : function($scope, $element, $attribute){
			
		}
	}
}]);